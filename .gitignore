__pycache__/
*.pyc
.venv/
*.log
*.egg-info/
build/
dist/
.pytest_cache/
.DS_Store

# local agent guide — not published (kept locally, references the private workshop)
/CLAUDE.md
.gstack/

# Prefill-tax capture (raw session traces, may contain previews)
traces/

# `chad prove` scorecard rows — written to the invoking directory, per run
/results.json

# Polyglot eval kit (benchmarks/polyglot/README.md): fetched exercises, toolchain caches,
# trial workspaces, KV checkpoints, run output and publish bundles. Nothing a run produces
# is committed; published runs are listed in benchmarks/polyglot/RUNS.md.
benchmarks/polyglot/_*/
